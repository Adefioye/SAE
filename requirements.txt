# Runtime dependencies for scripts/train_two_seed_sae.py.
# SAELens installs its supported PyTorch, TransformerLens, Hugging Face,
# datasets, and Weights & Biases dependencies transitively.
sae-lens==6.46.0
python-dotenv>=1.0.1,<2.0.0
