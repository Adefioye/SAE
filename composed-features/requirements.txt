# Experiment runtime dependencies (used directly by the notebooks).
torch>=2.6
sae-lens==6.47.0
matplotlib>=3.8
pandas>=2.0
scipy>=1.11
tqdm>=4.66
ipython>=8
jinja2>=3.1

# Local notebook and test tooling. Colab supplies its own notebook frontend,
# but installing this complete file is also safe there.
jupyterlab>=4
ipykernel>=6
pytest>=8
